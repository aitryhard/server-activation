services:
  - type: web
    name: aivex-activation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port 10000

databases:
  - name: aivex-db
    databaseName: aivex
    plan: free
